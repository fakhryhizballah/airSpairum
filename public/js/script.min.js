const swal = $(".swal").data("swal"); swal && Swal.fire({ icon: "warning", title: "Oops...", text: swal, confirmButtonText: "Oke" }); const email = $(".email").data("email"); email && Swal.fire({ title: "Verifikasi email", text: "Segera cek email dan nomor telpon anda untuk mendapatkan saldo Air 2000 secara gratis", imageUrl: "https://cdn.spairum.my.id/img/message.svg", imageWidth: 200, imageHeight: 200, imageAlt: "Verifikasi email" }); const flashData = $(".flash-data").data("flashdata"); if (flashData) { Swal.mixin({ toast: !0, position: "top-end", showConfirmButton: !1, timer: 4500, timerProgressBar: !0, onOpen: e => { e.addEventListener("mouseenter", Swal.stopTimer), e.addEventListener("mouseleave", Swal.resumeTimer) } }).fire({ icon: "success", title: flashData }) } const flashError = $(".flash-Error").data("flashdata"); console.log(flashError), flashError && Swal.fire({ icon: "error", title: "Oops...", text: flashError }); const flashSuccess = $(".flash-Success").data("flashdata"); console.log(flashSuccess), flashSuccess && Swal.fire({ icon: "success", title: "Berhasil", text: flashSuccess }); const currentLocation = location.href, menuItem = document.querySelectorAll(".item"), menuLength = menuItem.length; for (let e = 0; e < menuLength; e++)menuItem[e].href === currentLocation && (menuItem[e].className = "btn btn-link-default active");